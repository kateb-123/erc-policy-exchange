# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (if used for tooling)
node_modules/
npm-debug.log*
*.log

# Local, machine-specific Claude settings (launch.json is kept — it's shareable)
.claude/settings.local.json

# Affinity transient lock files (the .af sources ARE tracked)
*.af~lock~
