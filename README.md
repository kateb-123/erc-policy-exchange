# erc-policy-exchange (forwarding only)

This repository exists to keep the old link alive:

    https://kateb-123.github.io/erc-policy-exchange/  ->  https://erc-policy-exchange.vercel.app/

`index.html` and `404.html` are the same page; between them they catch the
root and every deeper path, carry the path and query across, and forward.

The site itself, and its history, live in the private repository
`kateb-123/erc-policy-exchange-app`, which Vercel builds.
